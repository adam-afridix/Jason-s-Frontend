# Build settings
[build]
  publish = "dist"
  command = "npm run build"

# SPA routing - redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Environment variables (optional - can also set in Netlify UI)
[build.environment]
  NODE_VERSION = "18"

